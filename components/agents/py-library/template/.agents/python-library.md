# Python library agent guidance

Use the repository's pinned `uv` environment and run commands through `uv run --no-sync` after one locked synchronization.

Preserve the public package surface, generated artifact checks, and the repository's declared import and policy boundaries.
